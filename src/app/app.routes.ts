import { Routes } from '@angular/router';
import { ResumeFormsComponent } from './resume-forms/resume-forms.component';

export const routes: Routes = [
    {path: '', component: ResumeFormsComponent}
];
